# =============================================================================
# LOCAL DEVELOPMENT ENVIRONMENT (Docker Compose)
# =============================================================================
# Use these settings when running services locally with docker-compose

# AWS (for local testing - can be dummy values)
AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1
SQS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/dummy/local-queue

# Database (Docker PostgreSQL)
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASS=password
DB_NAME=postgres

# Redis (Docker Redis)
REDIS_HOST=redis
REDIS_PORT=6379

# OpenSearch (Docker OpenSearch)
OPENSEARCH_URL=http://opensearch:9200
