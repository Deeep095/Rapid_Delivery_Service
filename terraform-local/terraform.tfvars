# Find your IP by Googling "what is my IP"
my_ip = "0.0.0.0/0"  # UPDATE THIS with your IP for security

# Database password (used by Docker PostgreSQL)
db_password = "password123"

# AWS Region
aws_region = "us-east-1"

# Instance type for master (free tier eligible)
instance_type = "t3.micro"

# Instance type for worker (optional, can be removed)
worker_instance_type = "t3.micro"
